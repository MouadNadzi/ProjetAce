package com.codegen.controller;
/*
import com.codegen.service.GeminiCodeGeneratorService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.Map;

@RestController
@RequestMapping("/api/test")*/
public class TestController {
/*
    @Autowired
    private GeminiCodeGeneratorService geminiService;

    @PostMapping("/generate")
    public ResponseEntity<Map<String, String>> testGeneration(@RequestBody String umlContent) {
        try {
            Map<String, String> result = geminiService.generateModelClass(
                    umlContent,
                    "User",
                    "android",
                    "mvvm"
            );
            return ResponseEntity.ok(result);
        } catch (Exception e) {
            return ResponseEntity.badRequest().body(
                    Map.of("error", e.getMessage())
            );
        }
    }*/
}